# version: '3'
# services:
#   app:
#     container_name: qh
#     build:
#       context: .
#       dockerfile: Dockerfile
#     ports:
#       - "8080:8080"
#       - "8081:8081"
#       - "8082:8082"
#     restart: unless-stopped

version: "3"

services:
  crawler:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
       - "8080:8080"  
    command: ["./crawler", "crawl", "http://quotes.toscrape.com"]