root = "."
testdata_dir = "testdata"
tmp_dir = "./tmp"

[build]
  cmd = "dlv exec --accept-multiclient --headless --listen :${DELVE_PORT} --api-version 2 ./tmp/${APP_NAME}"
  bin = "./tmp/${APP_NAME}"
  # Otros ajustes de compilación y monitoreo
  delay = 1000
  exclude_dir = ["assets", "tmp", "vendor", "testdata"]
  exclude_file = []
  exclude_regex = ["_test.go"]
  include_ext = ["go", "tpl", "tmpl", "html"]
  log = "build-errors.log"
